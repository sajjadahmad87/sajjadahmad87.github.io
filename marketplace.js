const menu=document.querySelector('.menu');const links=document.querySelector('.nav-links');if(menu&&links){menu.addEventListener('click',()=>links.classList.toggle('open'));links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')))}document.querySelectorAll('[data-year]').forEach(x=>x.textContent=new Date().getFullYear());const search=document.querySelector('#courseSearch');const category=document.querySelector('#categoryFilter');const level=document.querySelector('#levelFilter');const cards=[...document.querySelectorAll('.course-card[data-title]')];function filterCourses(){const q=(search?.value||'').toLowerCase().trim();const c=category?.value||'';const l=level?.value||'';cards.forEach(card=>{const text=(card.dataset.title+' '+card.dataset.category+' '+card.textContent).toLowerCase();const okQ=!q||text.includes(q);const okC=!c||card.dataset.category===c;const okL=!l||card.dataset.level===l;card.classList.toggle('hidden',!(okQ&&okC&&okL))});const count=document.querySelector('#courseCount');if(count)count.textContent=cards.filter(x=>!x.classList.contains('hidden')).length+' courses shown'}[search,category,level].forEach(el=>el&&el.addEventListener(el.tagName==='INPUT'?'input':'change',filterCourses));document.querySelectorAll('[data-demo-action]').forEach(btn=>btn.addEventListener('click',e=>{const msg=btn.dataset.demoAction||'This feature will activate when the LMS backend is connected.';alert(msg)}));const instructorForm=document.querySelector('#instructorForm');if(instructorForm){instructorForm.addEventListener('submit',e=>{e.preventDefault();alert('Instructor application preview submitted. The production version will store this securely in the LMS backend.')})}