(()=>{
  const QUIZ_KEY='sea_lms_quiz_ppm_v1';
  const LMS_KEY='sea_lms_state_v1';
  const ACCOUNT_KEY='sea_account_v2';
  const SESSION_KEY='sea_session_v2';
  const COURSE_ID='preventive-maintenance-ppm';
  const QUESTIONS=[
    {q:'What makes a PPM checklist item most useful for maintenance quality?',a:['A broad instruction such as “check machine”','A clear inspection/action point with acceptance criteria or evidence to record where appropriate','As many checklist lines as possible','Only a technician signature'],c:1,e:'Useful PPM tasks are specific enough to guide the inspection and, where relevant, define what evidence, condition or acceptance criterion should be recorded.',r:'/guides/ppm-checklist/#complete-instruction'},
    {q:'A technician finds abnormal bearing noise during a scheduled PPM. What is the strongest next step?',a:['Tick the task complete because the PPM was performed','Ignore it unless production stops','Record the finding with evidence, assess risk/urgency, raise the required corrective action and track it to closure','Immediately replace the bearing without checking operating context or approved procedure'],c:2,e:'PPM is not complete simply because the inspection occurred. Abnormal findings should be documented, risk-assessed and converted into controlled corrective work with follow-up.',r:'/guides/ppm-checklist/#condition-escalation'},
    {q:'How should a preventive-maintenance interval be selected?',a:['Use the same monthly frequency for every asset','Base it on failure modes, OEM guidance, operating duty, condition/history, statutory/site requirements and risk, then review using evidence','Choose the longest interval to reduce labour','Change the interval whenever one technician prefers a different frequency'],c:1,e:'Intervals should reflect equipment risk and evidence. OEM requirements, duty, environment, failure history and mandatory/site controls all matter, and intervals should be reviewed when evidence changes.',r:'/guides/ppm-checklist/#frequency-evidence'},
    {q:'Which record gives the strongest evidence that a PPM task was genuinely completed?',a:['A generic “done” comment','A completed task with relevant readings/condition notes, defects raised where necessary, date and responsible person','A copied result from the previous month','A blank checklist signed at the end of the shift'],c:1,e:'Good maintenance records are traceable and show the actual condition or result. Copying previous readings or using generic completion comments weakens reliability analysis and auditability.',r:'/guides/ppm-checklist/#minimum-fields'},
    {q:'When should a recurring PPM task itself be improved?',a:['Never, because approved checklists must remain unchanged forever','Only after a major breakdown','When breakdown history, repeat defects, OEM updates, inspections or technician evidence show that the task, method, frequency or acceptance criteria are not controlling the relevant failure mode','Whenever a new template looks more attractive'],c:2,e:'PPM content should be controlled but not static. Evidence from failures, inspections, OEM updates and maintenance history should drive approved improvements to task scope, method, interval or acceptance criteria.',r:'/guides/ppm-checklist/#quality-audit'}
  ];
  const scoreAnswers=answers=>QUESTIONS.reduce((score,x,i)=>score+(answers[i]!==null&&answers[i]!==undefined&&Number(answers[i])===x.c?1:0),0);
  const resultMarkup=score=>{const pct=Math.round(score/QUESTIONS.length*100);return `<p><strong>Score: ${score}/${QUESTIONS.length} (${pct}%).</strong> ${pct>=80?'Strong result. Review any missed explanation and keep linking PPM findings to controlled follow-up actions.':'Review the explanations, use the resources below, and retry when ready.'}</p><nav class="lms-quiz-actions" aria-label="Recommended PPM review resources"><span>Next study:</span><a class="btn btn-secondary" href="/guides/ppm-checklist/">PPM checklist guide</a><a class="btn btn-secondary" href="/guides/preventive-maintenance/">Preventive maintenance guide</a></nav>`};
  const read=k=>{try{return JSON.parse(localStorage.getItem(k)||'null')}catch{return null}};
  const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
  const signedIn=()=>{const a=read(ACCOUNT_KEY),s=read(SESSION_KEY);return !!(a&&s&&a.email&&a.email===s.email)};
  const login=()=>{location.href='/signin.html?return='+encodeURIComponent(location.pathname+location.search+location.hash)};
  const quizState=()=>{const x=read(QUIZ_KEY)||{attempts:[]};x.attempts=Array.isArray(x.attempts)?x.attempts:[];return x};
  const bestScore=()=>{const a=quizState().attempts;return a.length?Math.max(...a.map(x=>Number(x.score)||0)):null};
  const addActivity=score=>{const s=read(LMS_KEY)||{enrolled:{},saved:[],progress:{},activity:[]};s.activity=Array.isArray(s.activity)?s.activity:[];s.activity.unshift({type:'quiz-attempt',courseId:COURSE_ID,label:`PPM knowledge check: ${score}/${QUESTIONS.length}`,at:new Date().toISOString()});s.activity=s.activity.slice(0,40);s.updatedAt=new Date().toISOString();write(LMS_KEY,s)};
  const renderSummary=()=>{const q=quizState(),b=bestScore();document.querySelectorAll('[data-lms-ppm-quiz-best]').forEach(el=>el.textContent=b===null?'Not attempted':`${b}/${QUESTIONS.length}`);document.querySelectorAll('[data-lms-ppm-quiz-attempts]').forEach(el=>el.textContent=String(q.attempts.length));};
  const mount=()=>{
    const root=document.querySelector('[data-lms-ppm-quiz]');if(!root)return;
    root.innerHTML=`<div class="lms-quiz-head"><div><span class="badge">KNOWLEDGE CHECK</span><h2>Preventive Maintenance & PPM Practical Check</h2><p>Five questions focused on task quality, findings, evidence, intervals and checklist improvement.</p></div><div class="lms-quiz-score"><small>Best score</small><strong data-lms-ppm-quiz-best>Not attempted</strong></div></div><form data-lms-ppm-quiz-form>${QUESTIONS.map((x,i)=>`<fieldset class="lms-quiz-question"><legend>${i+1}. ${x.q}</legend>${x.a.map((o,j)=>`<label><input type="radio" name="q${i}" value="${j}" aria-describedby="ppm-feedback-${i}"><span>${o}</span></label>`).join('')}<div class="lms-quiz-feedback" id="ppm-feedback-${i}" data-ppm-feedback="${i}" hidden></div></fieldset>`).join('')}<div class="lms-quiz-actions"><button class="btn btn-primary" type="submit">Submit PPM check</button><button class="btn btn-secondary" type="reset">Clear answers</button><span class="lms-local-note">Attempts on this browser: <strong data-lms-ppm-quiz-attempts>0</strong></span></div><div class="lms-quiz-result" data-lms-ppm-result hidden aria-live="polite" tabindex="-1"></div></form>`;
    renderSummary();
    const form=root.querySelector('[data-lms-ppm-quiz-form]');
    form.addEventListener('submit',e=>{
      e.preventDefault();if(!signedIn())return login();const fd=new FormData(form);let answered=0;const answers=[];
      QUESTIONS.forEach((x,i)=>{const raw=fd.get('q'+i),box=form.querySelector(`[data-ppm-feedback="${i}"]`);if(raw!==null){answered++;answers[i]=raw;const ok=Number(raw)===x.c;box.hidden=false;box.className='lms-quiz-feedback '+(ok?'ok':'needs-review');box.replaceChildren(document.createTextNode((ok?'Correct. ':'Review: ')+x.e));if(!ok){const link=document.createElement('a');link.className='lms-quiz-review-link';link.href=x.r;link.textContent='Review this topic';box.append(' ',link)}}else{box.hidden=false;box.className='lms-quiz-feedback needs-review';box.textContent='Select an answer before submitting.'}});
      const score=scoreAnswers(answers);const result=form.querySelector('[data-lms-ppm-result]');if(answered<QUESTIONS.length){result.hidden=false;result.textContent=`You answered ${answered} of ${QUESTIONS.length} questions. Complete all questions to save an attempt.`;const firstUnanswered=QUESTIONS.findIndex((_,i)=>fd.get('q'+i)===null);const firstInput=firstUnanswered>=0?form.querySelector(`input[name="q${firstUnanswered}"]`):null;if(firstInput)firstInput.focus();return}
      const state=quizState();state.attempts.unshift({courseId:COURSE_ID,score,total:QUESTIONS.length,at:new Date().toISOString()});state.attempts=state.attempts.slice(0,20);write(QUIZ_KEY,state);addActivity(score);renderSummary();result.hidden=false;result.innerHTML=resultMarkup(score);result.focus();
    });
    form.addEventListener('reset',()=>setTimeout(()=>{form.querySelectorAll('.lms-quiz-feedback').forEach(x=>{x.hidden=true;x.textContent=''});const r=form.querySelector('[data-lms-ppm-result]');r.hidden=true;r.textContent=''},0));
  };
  document.addEventListener('DOMContentLoaded',()=>{mount();renderSummary()});
})();
